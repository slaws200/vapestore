import { Product } from '@interfaces/Product';

export default interface Database {
    cartriges: Product[];
    pods: Product[];
    elfliq: Product[];
    chaser: Product[];
    octobar: Product[];
    vozol: Product[];
}